package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Bento Grid Design Theme Palette
val BentoBgLight = Color(0xFFFEF7FF)              // Very light lavender-white
val BentoTextDark = Color(0xFF1D1B20)             // Deep charcoal as main text
val BentoPrimary = Color(0xFF6750A4)              // Theme signature violet
val BentoPrimaryContainer = Color(0xFFEADDFF)     // Rich pastel lavender container
val BentoOnPrimaryContainer = Color(0xFF21005D)   // Royal purple text
val BentoOutline = Color(0xFFCAC4D0)              // Structural divider border
val BentoSurfaceLight = Color(0xFFFFFFFF)         // Clean white surfaces

// Bento Pastel Category Colors (matched to HTML design)
val BentoRose = Color(0xFFFFD8E4)                 // Soft rose pink (Dining / Shopping)
val BentoMint = Color(0xFFE6F4EA)                 // Soft mint green (Groceries / Transit)
val BentoLavender = Color(0xFFE8DEF8)             // Soft lavender-gray (Bills / Utilities)
val BentoSky = Color(0xFFE0F2FE)                  // Soft light blue (Entertainment)
val BentoAmber = Color(0xFFFEF3C7)                // Soft gold-yellow (Others)

// Midnight Bento Dark Theme equivalent
val BentoBgDark = Color(0xFF121115)               // Deep dark violet-black
val BentoTextLight = Color(0xFFE6E1E9)            // Light pastel silver text
val BentoPrimaryDark = Color(0xFFD0BCFF)          // Radiant lavender accent
val BentoSurfaceDark = Color(0xFF1D1B20)          // Midnight graphite surface
val BentoSurfaceVariantDark = Color(0xFF2B2930)   // Dark gray-lavender surface variant
val BentoOutlineDark = Color(0xFF49454F)          // Muted divider for dark theme
val BentoErrorLight = Color(0xFFB3261E)
val BentoErrorDark = Color(0xFFF2B8B5)
